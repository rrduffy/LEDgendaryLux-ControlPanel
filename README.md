# LED Control Panel

A Flutter-based mobile app for controlling modular RGB LED panels via Bluetooth.

## Features
- Layout mode: drag and arrange panels
- Color picker with RGB + brightness controls
- Multiple LED effects (Pulse, Trail, Rainbow)
- BLE initialization for connected panels

## Setup
1. Install Flutter (v3.16+)
2. Run:
   ```bash
   flutter pub get
   flutter run
